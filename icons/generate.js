/* Run this in browser console or as a script to generate PNG icons from SVG */
/* Open generate-icons.html to download icons, then place them in /icons/ */

// For PWA to work without PNG icons, we update manifest to use SVG
// Most modern browsers support SVG icons in manifest

console.log('Icons: Using SVG icon. Open generate-icons.html to create PNGs if needed.');
